{% extends "base.html.twig" %}

{% block body %}
	{% if app.user %}

		<h1>{{'welcome'|trans}}
		</h1>
		<h2>{{ app.user.email }}</h2>


	{% else %}

		<h1>{{'welcome'|trans}}</h1>

	{% endif %}

{% endblock %}
