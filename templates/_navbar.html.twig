<nav class="navbar navbar-expand-lg navbar-light ">
	<div class="container-md">
		<ul class="nav">
			<li class="nav-item">
				<a class="nav-link active" aria-current="page" href="{{ path('program_index') }}">WILD SERIES</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('category_index') }}">Catégories</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('season_index') }}">Saisons</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('episode_index') }}">Episodes</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('actor_index') }}">Acteurs</a>
			</li>
		</ul>
		{% if app.user %}

			{{ app.user.email }}
			<a href="{{ path('user_show') }}">Mon compte</a>
			<a href="{{ path('app_logout') }}">
				Déconnexion</a>

		{% else %}
			<a href="{{ path('app_register') }}">Inscription</a>
			<a href="{{ path('login') }}">
				Connection</a>
		{% endif %}

	</div>
</nav>
