<nav class="navbar navbar-expand-lg navbar-light ">
	<div class="container-md">
		<ul class="nav">
			<li class="nav-item">
				<a class="nav-link active" aria-current="page" href="{{ path('program_index') }}">WILD SERIES</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('category_index') }}">Catégories</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('season_index') }}">Saisons</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('episode_index') }}">Episodes</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('actor_index') }}">Acteurs</a>
			</li>
		</ul>

		{% if app.user %}

   		{% trans with {'%username%': app.user.email} %}
       		hello_username
   		{% endtrans %} !

			<a href="{{ path('user_show') }}">{{ 'account.profile'|trans }}</a>
			<a href="{{ path('app_logout') }}"> {{ 'account.logout'|trans }}</a>
		{% else %}
			<a href="{{ path('app_register') }}">{{ 'account.sign_in'|trans }}</a>
			<a href="{{ path('login') }}"> {{ 'account.login'|trans }}</a>
		{% endif %}
	</div>
</nav>
